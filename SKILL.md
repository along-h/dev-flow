---
name: dev-flow
description: >
  Use when a frontend development task needs requirement clarification, work-package decomposition,
  design input, implementation, risk-based review, parallel delivery, or evidence-backed verification.
---

# Dev Flow v2

Dev Flow 是面向前端交付的精简团队流程。Orchestrator 维护范围、依赖、用户决策和证据，只调度完成当前任务所需的角色，不让每个角色重复整条流程。

## 核心原则

- 用户结果、可验证验收和真实风险优先于产物数量与格式完整度。
- 所有修改限于用户确认的需求范围；范围外发现只报告，不顺手重构。
- 组件、Hook、状态和共享层按所有权、变化边界与稳定契约决定，不按出现次数或代码行数决定。
- 设计稿在需求阶段是理解需求的证据；是否补充模块具体设计稿，由用户在 UI 开发前选择。
- 新 v2 运行不生成设计覆盖矩阵、`DESIGN-SOURCES.md` 或模块设计规格，也不执行旧设计就绪门禁。
- 验证必须覆盖被改变的行为和真实风险；格式检查不能代替测试、类型检查、Lint 或构建。

## 角色路由

| Agent id | 何时调用 | 唯一责任 |
| --- | --- | --- |
| `project-scanner` | 受影响路径不明确时 | 定向定位入口、惯例、测试和真实复用候选 |
| `requirements-analyst` | 目标、行为、范围或验收存在高影响未知项时 | 用 grilling 收敛需求事实与用户决策 |
| `task-decomposer` | 存在多个交付边界、依赖或共享契约时 | 拆工作包，标记所有权、冲突、依赖和波次 |
| `architect` | 跨工作包共享契约、高风险状态模型或系统不变量需要独立判断时 | 给出最小共享方案和高风险约束 |
| `developer` | 每个已就绪工作包 | 测试驱动实现、定向验证和自检 |
| `code-reviewer` | 命中审查触发器或用户明确要求时 | 审查触发风险及直接影响范围 |

单工作包可由一个 Developer 连续完成方案、实现和验证。Multi 只描述执行拓扑，不自动提高每个工作包的治理深度。角色交接只发送当前工作包所需的目标、验收、拥有路径、只读依赖、契约、用户决策、风险和验证命令。

## 0. 接入与定向发现

1. 检查项目是否已初始化；缺少 `.dev-flow/scripts/scan-project.js` 时提示运行 `npx dev-flow init`。
2. 读取用户提供的需求、UC、设计资料和明确约束，建立事实、决定、假设、未知项四类记录。
3. 先搜索受影响入口、现有实现、相邻测试和项目惯例。只有真实跨模块复用调查才建立完整组件索引。
4. 未知项若会改变业务行为、数据契约、权限、验收或工作包边界，交给 Requirements Analyst 追问；其余采用项目既有惯例并记录假设。

## 1. 需求基线

需求基线只包含：目标、范围、非目标、主要行为、边界状态、验收标准、已确认决定和仍阻塞的未知项。

- 已有设计稿用于补充页面范围、文案、交互和状态，不判断“设计是否完整”。
- 不递归枚举全部可见组件，不要求组件级节点，不创建设计类中间产物。
- Requirements Analyst 只问会改变交付结果的问题；可批量回答的问题合并询问。
- 阻塞未知项清零后，由用户确认需求基线。未影响结果的视觉细节不阻塞拆分。

## 2. 工作包拆分

工作包是能独立实现、验证和回滚的交付单元。页面数、UC 数、目录数和预计代码量都不是拆分依据。

每个工作包记录：

```text
id, outcome, scope, nonGoals
ownedPathsOrContracts, readOnlyDependencies
dependsOn, parallelWave
acceptance, rollback
```

拆分规则：

- 一个路径或可写契约在同一波次只能有一个所有者。
- 有共享契约时，wave 0 只建立后续工作真正需要的最小类型、API、状态或稳定 UI 契约。
- 同一波次只有在依赖已满足、无共享写入且文件没有冲突时才并行。
- 同波次工作包不以逐包用户验收作为相互启动条件；在波次结束后统一集成和报告。
- 新依赖或冲突只暂停受影响的包，更新 `dependsOn` 与 `parallelWave`，不让无关工作回退。

单工作包使用精简 `PLAN.md`；多个工作包使用 `TASK-BREAKDOWN.md`，各 Developer 只接收自己的最小执行上下文。

## 3. 开发前设计选择

当前波次包含 UI 工作包时，Orchestrator 必须在 Developer 写测试或代码前统一询问一次：

> 以下工作包即将进入开发：{WP 列表}。是否提供这些模块的具体设计稿，还是按照目前已有的需求资料、设计稿和项目视觉规范继续？

记录合同：

```text
designMode: provided-specific | use-current-basis
designReferences: concrete links or existing project paths
appliesTo: work-package ids
manualVisualChecks: observable checks only
```

- `provided-specific`：用户将补充模块具体设计稿；只暂停受影响的 UI 工作包，收到资料后继续。
- `use-current-basis`：按已确认需求、已有资料和项目视觉惯例开发；纯视觉细节缺口不再追问或阻塞。
- 一个波次可以按工作包记录不同选择，但由 Orchestrator 合并提问，避免各 Developer 重复打断用户。
- 非 UI 工作包跳过此选择。之后只有业务行为、数据契约或验收标准缺失时才重新询问。

这一步是用户选择，不是 Agent 自评设计就绪度。无论用户选择哪种模式，后续都不生成设计覆盖矩阵。

## 4. 治理路由

| 深度 | 适用证据 | 行为 |
| --- | --- | --- |
| Fast | 局部、明确、低风险且易回滚 | Developer 使用一份短 PLAN 完成实现与验证 |
| Standard | 存在真实中风险、依赖或外部副作用 | Technical Lead 定向检查风险、依赖和回滚 |
| Rigorous | 权限、安全、不可逆操作、高技术不确定性或关键系统不变量 | Architect 给出独立高风险方案，Reviewer 必审 |

多工作包中的每个包独立按风险选择深度。新 Rigorous 运行仍使用本页的用户设计选择和精简产物，不恢复旧设计矩阵链。

以下情况触发 Reviewer：用户明确要求审查；权限、安全或不可逆变更；外部写入、重试、乱序或并发会改变结果；关键共享契约变化；高风险迁移；验证证据与验收冲突。未命中触发器时由 Developer 提供自检和运行证据后交付。

## 5. 实现与验证

Developer 按依赖波次工作：

1. 读取需求基线、当前工作包、`designMode`、拥有路径、契约、风险和验收。
2. 先写或修改能捕获行为变化的测试并确认测试因缺少目标行为而失败。
3. 在拥有边界内做最小实现；遵循项目现有目录、请求、错误提示和样式约定。
4. 运行定向测试，再运行适用的类型检查、Lint、构建和跨包集成验证。
5. 报告命令、退出码、结果摘要、未验证项和回滚方式；失败时不得宣称完成。

同一波次的 Developer 可以同时开始。波次完成后，Orchestrator 合并代码与契约验证，确认下一波依赖已满足；共享契约改变时只通知直接消费者并重新排受影响工作包。

## 6. 代码边界

### 组件

只有形成稳定、可命名、可独立变化的业务或交互概念时才抽组件。多个真实消费者依赖同一稳定契约、独立测试或渲染边界有实际价值，都是有效证据。仅为减少行数、消除表面重复或增加 Props 透传的拆分没有价值。

页面容器默认只负责路由、页面级错误边界、真实跨区域协调和模块组装。查询、表单、弹窗和局部交互状态不会因为位于页面中就自动提升。

### Hook

Hook 封装内聚的状态、副作用与生命周期，或向真实消费者提供稳定契约。一次使用的简单取值、格式化、事件处理和固定行数不是抽取依据；私有 Hook 与其所有者共置。

### 状态

状态归属到能维护其不变量的最小必要所有者：

- 单组件输入、展开和临时选择归该组件或共置 Hook。
- 多个兄弟共同修改的状态归最近的真实公共所有者。
- 可分享或刷新恢复的导航状态归 URL/Router。
- 服务端数据、加载和缓存归项目既有 server-state 工具。
- 跨距离消费且需跨页面生命周期保留的状态才进入既有 Store/Context。

### 性能

不在方案阶段预设 `React.memo`、`useMemo` 或 `useCallback` 热点。只有测量证明真实瓶颈，或稳定引用是已确认的组件/库契约时才加入；Reviewer 同时指出缺少证据支持的优化和没有证据的优化。

## 7. 产物与预算

| 场景 | 默认产物 |
| --- | --- |
| 机械、非 UI、低风险小改动 | 无方案文件；保留 diff 与运行证据 |
| 单工作包 | 一份 `PLAN.md` |
| 多工作包 | 一份 `TASK-BREAKDOWN.md` 加各包最小上下文 |
| 共享契约 | 只记录实际共享的类型、API、状态或 UI 契约 |
| 条件审查 | 无问题只返回结论；有问题才持久化 |

- Fast PLAN 目标不超过 80 行，Standard 不超过 120 行；这是提示词和产物预算，不是代码拆分规则。
- 单角色连续执行不创建 HANDOFF；只有跨角色中断恢复确有需要时才记录最小交接。
- 默认上下文只加载本 `SKILL.md`、被调度角色文件、当前计划和必要代码路径，不加载未使用角色与旧版资料。

## 8. 恢复与兼容

- 测试或验证失败：保留失败证据，在当前工作包内修复；范围外失败只报告。
- Agent 超时或中断：Orchestrator 用最新提交、工作包状态和运行证据接管，不短轮询。
- 用户改变需求、设计选择或共享契约：只重算受影响范围、依赖和波次。
- 既有旧运行继续由保留的校验器和模板支持。只有用户明确要求恢复、验证或迁移旧 Rigorous 运行时，才加载 `references/legacy-rigorous.md`；新运行不得默认读取它。

## 交付检查

- 用户范围与验收均有对应变更或证据。
- 同波次不存在共享写入或重叠文件所有权。
- UI 工作包已记录用户的 `designMode`。
- 组件、Hook、状态与 memoization 决策能说明所有权、契约或证据。
- 必需测试与工程检查有本轮真实结果，失败和未验证风险已明确报告。
