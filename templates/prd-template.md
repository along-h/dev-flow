# PRD（产品需求文档）

> 生成时间：{timestamp}
> 需求来源：{文档链接 / 设计稿链接 / 用户描述}
> 版本：v1.0

---

## 1. 概述

### 1.1 背景

（为什么要做这个功能？解决了什么用户痛点？）

### 1.2 目标

- 用户目标：
- 业务目标：
- 技术目标（如有）：

### 1.3 范围

**本次包含：**
- ...

**本次不包含：**
- ...

---

## 2. 用户角色

| 角色 | 描述 | 关键特征 |
|------|------|---------|
| ... | ... | ... |

---

## 3. 用户故事

### Story 1: {标题}

> 作为 **{角色}**，我希望 **{功能}**，以便 **{价值}**。

**验收标准：**
- [ ] ...
- [ ] ...

**边界条件：**
- 空状态：
- 加载状态：
- 错误状态：
- 极端情况：

**交互细节：**
- hover：
- active：
- focus：
- disabled：
- 键盘操作：

---

### Story N: {标题}

（同上结构，每个用户故事一节）

---

## 4. 页面/模块清单

| 页面/模块 | 路由 | 涉及用户故事 | 优先级 |
|-----------|------|-------------|--------|
| ... | ... | Story 1,2 | P0 |

---

## 5. 设计规范

### 5.1 全局设计 Token（从顶层容器提取）

#### 颜色

| Token 名称 | 色值 | 用途 |
|-----------|------|------|
| `--color-primary` | #1677FF | 主色 |
| `--color-success` | #52C41A | 成功 |
| `--color-warning` | #FAAD14 | 警告 |
| `--color-error` | #FF4D4F | 错误 |
| `--color-text-primary` | #1F1F1F | 主要文字 |
| `--color-text-secondary` | #666666 | 次要文字 |
| `--color-bg-page` | #F5F5F5 | 页面背景 |
| `--color-bg-container` | #FFFFFF | 容器背景 |

#### 字体

| Token 名称 | font-family | font-size | font-weight | line-height |
|-----------|-------------|-----------|-------------|-------------|
| `--font-h1` | ... | 24px | 600 | 32px |
| `--font-h2` | ... | 20px | 600 | 28px |
| `--font-body` | ... | 14px | 400 | 22px |
| `--font-caption` | ... | 12px | 400 | 20px |

#### 间距

| Token 名称 | 值 |
|-----------|-----|
| `--spacing-xs` | 4px |
| `--spacing-sm` | 8px |
| `--spacing-md` | 16px |
| `--spacing-lg` | 24px |
| `--spacing-xl` | 32px |

#### 圆角

| Token 名称 | 值 |
|-----------|-----|
| `--radius-sm` | 4px |
| `--radius-md` | 8px |
| `--radius-lg` | 12px |

#### 阴影

| Token 名称 | 值 |
|-----------|-----|
| `--shadow-sm` | 0 1px 2px rgba(0,0,0,0.06) |
| `--shadow-md` | 0 4px 8px rgba(0,0,0,0.08) |
| `--shadow-lg` | 0 8px 24px rgba(0,0,0,0.12) |

#### 动效（如无法从设计稿提取，标注"待设计师确认"）

| Token 名称 | 值 |
|-----------|-----|
| `--transition-fast` | 150ms ease |
| `--transition-normal` | 300ms ease |

### 5.2 响应式断点

| 断点 | 最小宽度 | 设计稿宽度 |
|------|---------|-----------|
| 手机 | 320px | 375px |
| 平板 | 768px | 768px |
| 桌面 | 1024px | 1440px |
| 大屏 | 1440px | 1920px |

### 5.3 模块级设计规格（从模块链接精确提取）

> 每个模块一个子章节，标注 MasterGo 模块链接，供开发阶段 1:1 对照还原。

#### 模块：{模块名称}

- **MasterGo 链接**：{模块链接}
- **页面布局**：{整体布局描述}

**组件清单：**

| 组件 | 位置 | 关键规格 |
|------|------|---------|
| ... | ... | 宽度 x 高度，字号，颜色等 |

**组件状态：**

| 组件 | 状态 | 规格 |
|------|------|------|
| 按钮 | normal | 背景 #1677FF，文字 #FFF，高度 40px |
| 按钮 | hover | 背景 #4096FF |
| 按钮 | active | 背景 #0958D9 |
| 按钮 | disabled | 背景 #D9D9D9，文字 #999，cursor not-allowed |
| 按钮 | loading | 左侧 spinner + 文字 "加载中..." |

**间距规格：**

| 元素对 | 间距 |
|--------|------|
| 搜索框 ↔ 表格 | 16px |
| 表格行之间 | 无间距（border-bottom 分隔） |
| 分页 ↔ 表格 | 16px |

**文字处理：**

| 场景 | 处理方式 |
|------|---------|
| 表格列超长文字 | ellipsis 截断 + hover tooltip |
| 卡片标题 | 最多 2 行，超出 clamp |

---

#### 模块：{模块名称 2}

（同上结构，每个模块一节）

---

## 6. 非功能需求

### 6.1 浏览器兼容性

- [ ] Chrome（最新 2 个版本）
- [ ] Safari（最新 2 个版本）
- [ ] Edge（最新 2 个版本）
- [ ] 移动端浏览器（如适用）

### 6.2 性能基准

- 首屏加载时间：< {N}s
- 交互响应时间：< {N}ms
- Lighthouse Performance 评分：> {N}

### 6.3 无障碍（a11y）

- [ ] 语义化 HTML
- [ ] 键盘导航
- [ ] 屏幕阅读器友好
- [ ] 色彩对比度（WCAG AA）

### 6.4 国际化（如适用）

- 语言：
- 日期格式：
- 数字格式：

---

## 7. 附录

### 7.1 关键决策记录

| 决策 | 选项 | 选择 | 理由 | 日期 |
|------|------|------|------|------|
| ... | ... | ... | ... | ... |

### 7.2 待确认项

- [ ] ...