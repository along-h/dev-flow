# 模块设计规格：{模块名称}

> 工作包：{work_package_id}
> 更新时间：{timestamp}

## 1. 设计源

- **模块链接**：{可回查的精确设计稿链接}
- **顶层链接**：{顶层设计稿链接 / 无}
- **节点名称**：{精确节点名称}
- **节点范围**：{节点、Frame 或模块名称}
- **节点层级路径**：{页面 / Frame / 模块 / UI 组件}
- **提取时间**：{timestamp}
- **提取方式**：{直接模块链接 / 从顶层设计稿自行定位}

## 2. UI 组件与精确节点映射

| UI 组件 | 所在页面 | 精确节点名称 | 精确节点链接 | 节点层级路径 | 提取时间 | 适用状态 |
|---------|----------|--------------|--------------|--------------|----------|----------|
| {组件名称} | {页面名称} | {节点名称} | {可回查链接} | {页面 / Frame / 模块 / UI 组件} | {timestamp} | {normal/hover/active/focus/disabled/loading/empty/error 中适用项} |

每个可见 UI 组件都必须映射到含 `node-id`、`nodeId` 或 `node` 参数/片段的精确子节点并独立列出适用状态。提取结论为 `complete` 时，节点层级、适用状态、响应式规则和文字溢出不得填写“无”或“不适用”。非视觉文件不进入本表；无法确认的视觉事实必须保留为 `blocked`，不得通过相似页面或经验推断。

本规格用于 `required` 设计源的精确节点提取；`inactive` 分支直接在 `COMPONENTS.md` 与 `DESIGN-SOURCES.md` 记录可回查的项目视觉基线路径，不伪造设计子节点。

## 3. 证据引用

- **COMPONENTS 设计覆盖**：`COMPONENTS.md v{N}`，UI 组件：{与映射表完全一致的组件名清单}
- **DESIGN-SOURCES 记录**：`DESIGN-SOURCES.md`，UI 组件：{与映射表完全一致的组件名清单}

运行产物必须填写具体版本和组件名；两个引用中的组件名必须与“UI 组件与精确节点映射”逐项一致，不得填写“见上文”或空语义。

## 4. 布局与尺寸

| 区域/组件 | 布局 | 宽度 | 高度 | 间距 | 对齐 |
|-----------|------|------|------|------|------|
| {名称} | {flex/grid/absolute} | {值} | {值} | {值} | {值} |

## 5. 设计 Token

| 类别 | Token/属性 | 值 | 来源节点 |
|------|------------|----|----------|
| 颜色 | {属性} | {值} | {节点} |
| 字体 | {属性} | {值} | {节点} |
| 间距 | {属性} | {值} | {节点} |
| 圆角 | {属性} | {值} | {节点} |
| 阴影 | {属性} | {值} | {节点} |

## 6. 组件状态

| UI 组件 | 状态 | 规格 | 证据 |
|-----------|------|------|------|
| {组件} | normal | {布局、颜色、字体、间距与交互规格} | {精确状态节点或可回查证据} |
| {组件} | {hover/active/focus/disabled/loading/empty/error 中的适用状态} | {该状态的具体规格} | {精确状态节点或可回查证据} |

“UI 组件与精确节点映射”的适用状态必须在本表逐状态各占一行。只罗列状态名称、合并多个状态到一格、缺少规格或证据都不允许；确实不适用的状态不写入适用状态清单，也不伪造证据行。

## 7. 文字与溢出

| 场景 | 字体规格 | 最大行数/宽度 | 溢出处理 |
|------|----------|---------------|----------|
| {场景} | {规格} | {限制} | {ellipsis/clamp/换行} |

## 8. 响应式规则

| 断点 | 布局变化 | 尺寸/间距变化 |
|------|----------|---------------|
| {断点} | {规则/不适用} | {规则/不适用} |

## 9. 提取完整度

- **结论**：`complete` / `incomplete` / `blocked`
- **未确认项**：{清单 / 无}
- **需要用户补充的链接**：{清单 / 无}
- **允许进入开发**：{是 / 否}
