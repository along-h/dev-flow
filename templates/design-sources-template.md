# 设计源登记表

> 更新时间：{timestamp}
> 当前工作包：{work_package_id / 尚未拆分}

## 1. 状态判定

- **当前状态**：`inactive` / `required`（运行产物只能保留一个精确值，不得拼接其他状态）
- **判定依据**：{未提供任何设计源 / 已提供顶层、模块或组件设计源链接}
- **设计源证据**：{链接或“当前任务及有效上下文均无设计源”}
- **状态优先级**：存在任意设计源 → `required`；确无任何设计源 → `inactive`
## 2. 当前任务范围

- 状态唯一性规则：运行产物全文只能保留一个状态判定章节（`## 状态判定` 或 `## 1. 状态判定`）和一个 `当前状态` 字段；规则说明中的自然语言“当前状态”不计作字段，状态判定章节不得记录任务级豁免
- 逐组件豁免规则：`waived` 不是任务级设计源状态，只能在 `COMPONENTS.md` 设计覆盖矩阵中对明确点名的 UI 组件记录用户原话摘要、残余风险和人工视觉验收范围
- 涉及页面：{页面清单}
- 涉及模块：{模块清单}
- 当前工作包：{WP 编号或尚未拆分}

## 3. 模块设计源清单与组件节点映射

| 模块 | 页面 | UI 组件 | 工作包 | 顶层设计源 | 精确组件节点 | 节点层级路径 | 适用状态 | 完整度 | 关联模块规格 | 提取时间 | 响应式差异 | 文字溢出 |
|------|------|---------|--------|------------|--------------|--------------|----------|--------|--------------|----------|------------|----------|
| {模块名称} | {页面名称} | {组件名称} | {WP01} | {链接 / 无} | {精确节点链接 / 项目视觉基线：真实组件或样式路径 / 自动定位 / 缺失 / 不适用} | {页面 / Frame / 组件节点 / 不适用} | {normal/hover/active/focus/disabled/loading/empty/error 中适用项 / 不适用} | {complete / blocked / waived / not-applicable} | {.dev-flow/runs/{需求编号}/design/模块名.md / 无（inactive）} | {timestamp} | {断点差异 / 沿用现有断点 / 不适用} | {ellipsis/clamp/换行 / 不适用} |

每个可见 UI 组件单独占一行。`required + complete` 的顶层设计源、含节点参数的精确组件节点、节点层级路径、提取时间、适用状态、响应式差异和文字溢出都必须是非空语义证据；`inactive + complete` 必须保存项目 cwd 内真实存在的视觉基线路径；`not-applicable` 另按非视觉分支填写。非视觉文件不得询问用户；`waived` 行必须在下方记录指定组件、用户原话摘要、残余风险和人工视觉验收范围。

`UI 组件` 必须与 `COMPONENTS.md` 设计覆盖矩阵的视觉行名称一致；`精确组件节点`、`节点层级路径`、`适用状态`、`提取时间` 与 `关联模块规格` 必须和模块规格的组件映射及“证据引用”字段逐项一致，不能只写模块级笼统链接。

## 4. 缺失与处置

- 需要 Agent 从顶层设计源自动定位的精确子节点：{组件清单 / 无}
- 自动定位后仍为 `blocked` 的 UI 组件：{组件、页面、已有设计源、缺失事实、所需精确链接 / 无}
- 一次性向用户请求的精确组件链接：{集中问题记录 / 无}
- 逐组件豁免记录：{组件名称 + 用户原话 + 残余风险 + 人工视觉验收范围 / 无}
- 阻塞原因：{说明 / 无}

## 5. 刷新记录

| 时间 | 工作包 | UI 组件 | 操作 | 结果 |
|------|--------|---------|------|------|
| {timestamp} | {WP01} | {组件名称 / 全部} | {读取顶层设计稿 / 自动定位精确子节点 / 重新提取 / 用户逐项豁免} | {节点链接、完整度或阻塞结果} |

以上刷新记录必须能回查到当前设计覆盖矩阵与关联模块规格。
